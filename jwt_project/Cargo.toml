[package]
name = "jwt_project"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
jsonwebtoken = "8.3"
reqwest = { version = "0.11", features = ["blocking", "json"] }
